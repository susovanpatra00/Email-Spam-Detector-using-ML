import setuptools

with open("README.md", "r", encoding="utf-8") as f:
    long_description = f.read()


__version__ = "0.0.0"

REPO_NAME = "Email-Spam-Detector-Using-ML"
AUTHOR_USER_NAME = "susovanpatra00"
SRC_REPO = "mlProject"
AUTHOR_EMAIL = "susovanpatra00@gmail.com"


setuptools.setup(
    name=SRC_REPO,
    version=__version__,
    author=AUTHOR_USER_NAME,
    author_email=AUTHOR_EMAIL,
    description="A small python package for CNN app",
    long_description=long_description,
    long_description_content="text/markdown",
    url=f"https://github.com/{AUTHOR_USER_NAME}/{REPO_NAME}",
    project_urls={
        "Bug Tracker": f"https://github.com/{AUTHOR_USER_NAME}/{REPO_NAME}/issues",
    },
    package_dir={"": "src"},
    packages=setuptools.find_packages(where="src")
)



"""
This setup.py will remain same for every project,,just the REPO_NAME, AUTHOR_USER_NAME, SRC_REPO &
AUTHOR_EMAIL will change.
"""